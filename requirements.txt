# ============================================================
# Firefly Character AI Assistant — Core Dependencies
# ============================================================
# Install with: pip install -r requirements.txt
#
# For GPU training, first install PyTorch with CUDA:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124

# --- Core ML ---
torch>=2.6.0
transformers>=4.51.0,<5.0.0
peft>=0.15.0
trl>=0.13.0
accelerate>=0.34.0
bitsandbytes>=0.45.0
datasets>=2.21.0

# --- Backend API ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0
pydantic>=2.0.0
sse-starlette>=1.8.0

# --- RAG ---
chromadb>=0.5.0
sentence-transformers>=3.0.0

# --- Optional: Production ---
# vllm>=0.6.0           # High-performance inference engine
# slowapi>=0.1.9         # Rate limiting
# structlog>=24.0.0      # Enhanced structured logging
